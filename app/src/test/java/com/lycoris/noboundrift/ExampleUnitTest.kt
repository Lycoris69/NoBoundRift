package com.lycoris.noboundrift

import org.junit.Assert.assertEquals
import org.junit.Test

/** Placeholder unit test. Replace with real use-case tests. */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
